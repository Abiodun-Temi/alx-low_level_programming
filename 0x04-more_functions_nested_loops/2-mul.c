#include "main.h"
/**
 * mul - name function to multiply 2 intergers
 * @a: one of the variable to store value for multiplication
 * @b: second variable to store second num for multiplication
 * Return: return a multipied by b
 */
int mul(int a, int b)
{
	return (a * b);
}

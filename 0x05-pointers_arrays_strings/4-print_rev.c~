#include "main.h"
#include <string.h
/**
 * print_rev - print string in reverse
 * @s: print reverse variable
 * Return: return nothing
 */
void print_rev(char *s)
{
	int t;

	t = strlen(s);
	for ( i = t - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
}

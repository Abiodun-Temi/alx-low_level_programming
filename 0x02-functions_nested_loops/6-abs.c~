#include "main.h"
/**
 * _abs(int) - prints absolute value of an interger
 *@int: test for absolute value
 *Return: return absolute value of a number
 */
int _abs(int)
{
	if (n < 0)
		n = -n;
	return (n);


}

#include <stdio.h>
/**
 * main - main function
 *
 * Return: End the program
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; ++alphabet)
	  printf("%c", alphabet);
	return (0);
}
